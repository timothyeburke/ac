dependencies:
  pre:
    - npm install

  post:
    - grunt sass
    - grunt traceur
    - grunt concat
    - grunt uglify
    - webdriver-manager update

test:
  override:
    - grunt jshint
    - grunt jsbeautifier:verify
    - npm test
    - http-server:
      background: true
    - protractor protractor.conf.js

dependencies:
  pre:
    - npm install

  post:
    - grunt sass
    - grunt traceur
    - grunt concat
    - grunt uglify

test:
  override:
    - grunt jshint
    - grunt jsbeautifier:verify
    - npm test
